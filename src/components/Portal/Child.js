import React from 'react';

export class Child extends React.Component {
    render() {
        return (
           <div className={'modal'}>
               <button>Click</button>
           </div>
        )
    }
}